[pytest]
# 테스트 파일·함수 자동 발견 규칙
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 프로젝트 루트를 sys.path에 추가 (app 패키지 import 가능)
pythonpath = .

# 출력 옵션: 짧은 traceback, 컬러 강조
addopts = -ra --tb=short --strict-markers
